<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- TXT -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.txt" android:mimeType="text/plain" />

                <!-- MD -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.md" android:mimeType="text/markdown" />

                <!-- JSON -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.json" android:mimeType="application/json" />

                <!-- SELUNA -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.seluna" android:mimeType="application/seluna" />

                <!-- HTML (optional) -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.html" android:mimeType="text/html" />

                <!-- CSV (optional) -->
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.csv" android:mimeType="text/csv" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
